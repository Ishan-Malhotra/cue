# TMDB credential — server-side only, never exposed to the client.
# Accepts either a v3 API key (short) or a v4 read access token (long JWT).
# Copy this file to .env.local and fill in your value.
TMDB_API_KEY=
